import React from 'react';
import {MoviesList} from "../components/MovieList/MovieList";

const MoviesPage = () => {
    return (
        <>
            <MoviesList/>
        </>
    );
};

export {MoviesPage};